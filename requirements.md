# GroundView iOS 요구사항

## 기능 요구사항
- WDA 연동
  - 지정된 디바이스에 대해 `http://<ip>:8100/status` 헬스 체크.
  - 디바이스 리스트: `xcrun xctrace list devices`(기본), `idevice_id -l`(보조) 결과 표시.
  - 캡처: WDA `/screenshot` + `/source`(json/xml) 호출, 응답 지연/타임아웃 처리.
- 정합/분석
  - 스크린샷·트리 좌표 정규화, 텍스트 병합, 박스 매칭(NMS/IoU).
  - 요소 캐논 구조 생성(역할/텍스트/좌표/고유키).
- 로케이터/코드 생성
  - 우선순위: Accessibility Id > iOS Predicate > iOS Class Chain > 좌표/이미지 fallback, XPath는 최후 수단.
  - Appium 1/2/3 + Python/Java 코드 스니펫과 버튼 전수 클릭 스크립트 생성.
- UI/UX
  - 온보딩 iOS READY 카드: Xcode/WDA 안내, 상태/버전 표시.
  - 디바이스 선택/캡처 → 메인 분석 뷰(스크린샷+트리, 코드 탭).
  - 히스토리/비교, 덤프 ZIP 내보내기/불러오기(확장 목표).
- 설정/가이드
  - WDA 수동 빌드/서명/포트 설정 안내.
  - 오류 시 포트/서명/신뢰 설정 점검 문구와 재시도 버튼 제공.

## 비기능 요구사항
- 플랫폼: macOS 전용(iOS 기능), Electron 데스크톱 앱.
- 성능: 한 화면 캡처→분석 <3초(목표), OCR/모델은 경량화 또는 백엔드 프로세스.
- 오프라인 기본 동작, 언어팩/모델 번들.
- 로그: WDA 호출/에러, 로케이터 생성 결과를 파일/앱 로그에 기록.
- 확장성: `core` 모듈과 의존성 분리(포트/어댑터), iOS 전용 어댑터만 교체 가능.

## 제약/전제
- WDA는 사용자가 Xcode에서 수동 빌드/실행해야 함(자동 서명/설치는 범위 외).
- Windows 환경에서는 iOS 기능 비활성 안내만 제공.
- 회사/MDM 보안 정책으로 인한 포트 차단 시 사용자 안내만 제공(자동 우회 불가).
